// index.js - purpose and description here
// Author: Your Name
// Date:

// Constants

// Functions
// var oldEl = document.getElementsByClassName("shakytext");
// var myString = "Hello World";

// var draw_x = 32;
// var draw_y = 32;

// var intensity = 2; //shakeyness

// //get how long the string is in terms of width
// var length = string_length(string(myString));

// for(var i=1;i<=length;i++) { //loop through each character of the string
//     var str = string_copy(myString,i,1); //1 character of the string 
//     var length_so_far = string_width(string_copy(myString,1,i)); //how far to offset the text
//     draw_text(draw_x+length_so_far+random_range(-intensity,intensity),draw_y+random_range(-intensity,intensity),str);
// }

// const textElement = document.getElementById('ut-text');
// const text = textElement.textContent;
// textElement.textContent = '';

// var draw_x = 32;
// var draw_y = 32;

// var intensity = 2; //shakeyness
// for (const char of text) {
//   const span = document.createElement('span');
//    for(var i=1;i<=length;i++) { //loop through each character of the string
//      var str = string_copy(span,i,1); //1 character of the string 
//      var length_so_far = string_width(string_copy(span,1,i)); //how far to offset the text
//      draw_text(draw_x+length_so_far+random_range(-intensity,intensity),draw_y+random_range(-intensity,intensity),str);
// }
// span.textContent = char;
// textElement.appendChild(span);
// }

// oldEl.appendChild(myString);